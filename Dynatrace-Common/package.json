{
  "name": "dynatrace-common",
  "version": "0.1.0",
  "description": "Common code for all Dynatrace resource types",
  "private": true,
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npx tsc",
    "watch": "npx tsc -w",
    "prepack": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@amazon-web-services-cloudformation/cloudformation-cli-typescript-lib": "^1.0.1",
    "axios": "^0.27.2"
  },
  "devDependencies": {
    "@types/node": "^12.0.0",
    "typescript": "^4.1.2"
  }
}
